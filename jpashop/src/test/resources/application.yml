# test/resources/application.yml 없으면 src/resources/application.yml 읽음
# 스프링 부트는 datasource 설정이 없으면, 기본적으로 메모리 DB 사용하고, driver-class도 등록된 라이브러리 기준으로 맞춰줌
spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        #          show_sql: true
#        format_sql: true


logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace
